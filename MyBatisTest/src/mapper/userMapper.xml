<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="userSQL">
 	<insert id="write" parameterType="user.bean.UserDTO">
 		INSERT INTO usertable VALUES(#{name}, #{id}, #{pwd})
 	</insert>
 	
 	<select id="getUserList" resultType="user.bean.UserDTO">
 		SELECT * FROM usertable
 	</select>
 	
 	<update id="updateUser" parameterType="user.bean.UserDTO">
 		UPDATE usertable SET name = #{name}, id = #{id}, pwd = #{pwd} WHERE id = #{id}
 	</update>
 	
 	<delete id="deleteUser" parameterType="user.bean.UserDTO">
 		DELETE FROM usertable WHERE id = #{id}
 	</delete>
 	
 </mapper>